import {
    Paper, Typography
} from '@mui/material';

export default function AddNewChatItem() {
    return(
        <Paper elevation={0}>
            <Typography>
                AddNewChatItem
            </Typography>
        </Paper>
    );
}